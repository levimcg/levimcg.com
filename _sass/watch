# A nice shortcut for watching your Sass files from csswizardry.com
# Change file names to match your project. cd into the 'stylesheets' folder and 'sh watch.sh' to watch and compile you .scss files

# Expanded
sass --watch style.scss:../css/style.css --style expanded

# Minified
#sass --watch style.scss:..css/style.min.css --style compressed

exit 0