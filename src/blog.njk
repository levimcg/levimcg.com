---
title: Blog
description: A long-running collection of notes and and a few articles I've managed to write down. Grab my <a href="/feed.xml">RSS feed</a> if that’s your thing.
layout: layouts/base.njk
bodyClass: blog
hero:
  url: "./src/img/hero-image-2.png"
  altText: "Abstract illustration"
  idealWidth: 700px
pagination:
  data: collections.post
  size: 8
  reverse: true
eleventyNavigation:
  parent: Main
  key: Blog
  order: 1
---
<div class="layout-container">
  <div class="layout-container__inner [ p-top-600 p-bottom-500 ]">
    <div class="content-container">
      {% include "partials/article-header.njk" %}
    </div>
  </div>
</div>

<div class="layout-container">
  <div class="layout-container__inner [ p-top-600 p-bottom-700 ]">
    <div class="sidebar-layout content-container">
      <div class="flow">
        <ol class="list-reset post-list">
          {% for post in pagination.items %}
          <li>
            <div class="post-teaser flow">
              <a class="post-teaser__title" href="{{ post.url }}">{{ post.data.title }}</a>
              {% if post.data.description %}
              <p class="post-teaser__description">{{ post.data.description }}</p>
              {% endif %}
              <div class="post-teaser__date">
                <svg aria-hidden="true" width="20" height="20">
                  <use href="#calendar"></use>
                </svg>
                <time class="dt-published" datetime="{{ post.date | datetimeFormat }}">{{ post.date | dateFormatted }}</time>
              </div>
            </div>
          </li>
          {% endfor %}
        </ol>
        <div class="pagination">
          {% if pagination.href.previous %}
          <a class="m-top-500" href="{{ pagination.href.previous }}">← Newer posts</a>
          {% endif %}
          {% if pagination.href.next %}
          <a class="m-top-500" href="{{ pagination.href.next }}">Older posts →</a>
          {% endif %}
        </div>
      </div>
      <div class="flow">
        <h2>Tags</h2>
        <div class="tag-list m-top-100">
          <ul role="list" class="list-reset">
            {% for tag in collections.tagList %}
            <li>
              <a class="font-size-000 color-accent-primary" href="/tags/{{ tag | slugify }}/">#{{ tag }}</a>
            </li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>